# Treinamento-bot


## Instalação de dependências pasta por pasta
npm install -g pm2

cd c:\Treinamento-bot
npm install

cd SistemaPrincipal
npm install

cd front-end
npm install

cd ..\TemplatesMensagens
npm install

cd ..\..\wppconnect-server
npm install



## Instalação de dependências tudo de uma vez só

```bash
npm install -g pm2 && cd c:\Treinamento-bot && npm install && cd SistemaPrincipal && npm install && cd front-end && npm install && cd ..\TemplatesMensagens && npm install && cd ..\..\wppconnect-server && npm install``` 





